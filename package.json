{
  "name": "start-of-glow",
  "version": "0.1.0",
  "private": true,
  "description": "Start of Glow - the canonical Run by AI game-off codebase",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc --noEmit",
    "test": "playwright test",
    "check": "node scripts/check-workspace.mjs && npm run typecheck"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^26.2.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
